<core:FragmentDefinition
    xmlns:core="sap.ui.core"
    xmlns:l="sap.ui.layout"
    xmlns="sap.m">
    <Dialog
        title="{i18n>dialogCreateTitle}"
        contentWidth="40rem"
        resizable="true">
        <content>
            <core:Fragment fragmentName="project1.view.pages.main.fragments.ProductFieldsV4" type="XML" />
        </content>
        <beginButton>
            <Button
            text="{i18n>addButton}"
            type="Emphasized"
            enabled="{modelV4>/validate/isFormValid}"
            press="addProductRecordV4"/>
        </beginButton>
        <endButton>
            <Button text="{i18n>cancelButton}" type="Transparent" press="onCloseAddRecordProductDialogV4"/>
        </endButton>
    </Dialog>
</core:FragmentDefinition>