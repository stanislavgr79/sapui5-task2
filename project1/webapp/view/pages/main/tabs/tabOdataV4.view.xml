<mvc:View
    controllerName="project1.controller.Main"
    displayBlock="true"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc">
    <Table
        id="productsTableV4"
        class="sapUiResponsiveMargin"
        mode="MultiSelect"
        selectionChange="onSelectionChangeV4"
        width="auto"
        items="{ path: 'v4>/Products' }">
        <headerToolbar>
            <OverflowToolbar>
                <Title text="Products (V4)" level="H2" />
                <ToolbarSpacer />
                <Button 
                    text="{i18n>deleteRecordsButton}" press=".onDeleteV4Press" type="Reject"
                    enabled="{= ${modelV4>/selectedItems} > 0}" />
            </OverflowToolbar>
        </headerToolbar>
        <columns>
            <Column width="4em">
                <Text text="ID" />
            </Column>
            <Column minScreenWidth="Tablet" demandPopin="true">
                <Text text="Name" />
            </Column>
            <Column minScreenWidth="Tablet" demandPopin="true">
                <Text text="Description" />
            </Column>
            <Column minScreenWidth="Desktop" demandPopin="true">
                <Text text="ReleaseDate" />
            </Column>
            <Column hAlign="End">
                <Text text="Price" />
            </Column>
            <Column hAlign="Center">
                <Text text="Rating" />
            </Column>
        </columns>
        <items>
            <ColumnListItem vAlign="Middle">
                <cells>
                    <Text text="{v4>ID}" />
                    <Text text="{v4>Name}" />
                    <Text text="{v4>Description}" />
                    <Text 
                        text="{
                            path: 'v4>ReleaseDate',
                            type: 'sap.ui.model.odata.type.DateTimeOffset',
                            targetType:'any'
                        }"
                     />
                    <ObjectNumber number="{v4>Price}" />
                    <Text text="{v4>Rating}" />
                </cells>
            </ColumnListItem>
        </items>
    </Table>
</mvc:View>